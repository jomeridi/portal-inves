---
import { NAVIGATION, SOCIAL_LINKS, SITE } from '@config/site';
import { Package, Linkedin, Twitter, Facebook } from 'lucide-react';
import { languages, ui } from '../i18n/ui';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const currentPathname = Astro.url.pathname;

// Remove base from start if present to get pure path (Locale detection fix)
const purePath = currentPathname.startsWith(base) && base.length > 0 
  ? currentPathname.slice(base.length) 
  : currentPathname;

const segments = purePath.split('/').filter(Boolean);
const currentLocale = (segments[0] && Object.keys(languages).includes(segments[0])) ? segments[0] : 'es';

const rawNavItems = NAVIGATION[currentLocale as keyof typeof NAVIGATION] || NAVIGATION.es;
const siteInfo = SITE[currentLocale as keyof typeof SITE] || SITE.es;
const t = ui[currentLocale as keyof typeof ui] || ui.es;

const formatUrl = (url: string) => {
  return base + url;
};

const navItems = rawNavItems.map(item => ({
  ...item, 
  href: formatUrl(item.href)
}));
---

<footer class="bg-slate-50 text-slate-700 border-t border-slate-200">
  <div class="container mx-auto px-4 py-16 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 gap-12 lg:grid-cols-4">
      <!-- Company Info -->
      <!-- Funding Logos & Institutional Info -->
      <div class="lg:col-span-1 space-y-6">
        <div class="flex flex-wrap gap-6 items-center">
            <!-- Funding Logos -->
            <div>
                <img 
                  src={base + '/logos/MCIU.Gob.Web.svg'} 
                  alt="Ministerio de Ciencia, Innovación y Universidades" 
                  class="w-auto h-auto"
                />
            </div>
             <div>
                <a href="https://next-generation-eu.europa.eu/index_es" target="_blank" rel="noopener noreferrer">
                    <img 
                      src={base + '/logos/NextGenerationEU_es.png'} 
                      alt="Financiado por la Unión Europea - NextGenerationEU" 
                      class="w-auto h-auto"
                    />
                </a>
            </div>
            <div>
                <a href="https://planderecuperacion.gob.es/" target="_blank" rel="noopener noreferrer">
                    <img 
                      src={base + `/logos/logo-PRTR_${currentLocale}.png`} 
                      alt="Plan de Recuperación, Transformación y Resiliencia" 
                      class="w-auto h-auto"
                    />
                </a>
            </div>
        </div>
        
        <p class="text-sm text-slate-600 leading-relaxed font-medium">
          {t['footer.dialnet']}
        </p>
      </div>

      <!-- Quick Links -->
      <div>
        <h3 class="text-sm font-bold uppercase tracking-wider text-slate-900 mb-6">
          {t['footer.quickLinks']}
        </h3>
        <ul class="space-y-3">
          {navItems.map((item) => (
            <li>
              <a
                href={item.href}
                class="text-sm text-slate-600 hover:text-blue-600 transition-colors inline-flex items-center group"
              >
                <span class="w-0 group-hover:w-2 h-0.5 bg-blue-600 transition-all mr-0 group-hover:mr-2"></span>
                {item.name}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Services -->
      <div>
        <h3 class="text-sm font-bold uppercase tracking-wider text-slate-900 mb-6">
          Services
        </h3>
        <ul class="space-y-3">
          <li>
            <a href="/capabilities" class="text-sm text-slate-600 hover:text-blue-600 transition-colors inline-flex items-center group">
              <span class="w-0 group-hover:w-2 h-0.5 bg-blue-600 transition-all mr-0 group-hover:mr-2"></span>
              Warehousing & Storage
            </a>
          </li>
          <li>
            <a href="/capabilities" class="text-sm text-slate-600 hover:text-blue-600 transition-colors inline-flex items-center group">
              <span class="w-0 group-hover:w-2 h-0.5 bg-blue-600 transition-all mr-0 group-hover:mr-2"></span>
              Transportation
            </a>
          </li>
          <li>
            <a href="/capabilities" class="text-sm text-slate-600 hover:text-blue-600 transition-colors inline-flex items-center group">
              <span class="w-0 group-hover:w-2 h-0.5 bg-blue-600 transition-all mr-0 group-hover:mr-2"></span>
              Manufacturing
            </a>
          </li>
          <li>
            <a href="/capabilities" class="text-sm text-slate-600 hover:text-blue-600 transition-colors inline-flex items-center group">
              <span class="w-0 group-hover:w-2 h-0.5 bg-blue-600 transition-all mr-0 group-hover:mr-2"></span>
              Supply Chain Management
            </a>
          </li>
        </ul>
      </div>

      <!-- Contact -->
      <div>
        <h3 class="text-sm font-bold uppercase tracking-wider text-slate-900 mb-6">
          Get Started
        </h3>
        <ul class="space-y-3 text-sm">
          <li>
            <a href="/rfq" class="text-slate-600 hover:text-blue-600 transition-colors inline-flex items-center group">
              <span class="w-0 group-hover:w-2 h-0.5 bg-blue-600 transition-all mr-0 group-hover:mr-2"></span>
              Request a Quote
            </a>
          </li>
          <li>
            <a href="/facilities" class="text-slate-600 hover:text-blue-600 transition-colors inline-flex items-center group">
              <span class="w-0 group-hover:w-2 h-0.5 bg-blue-600 transition-all mr-0 group-hover:mr-2"></span>
              Find a Facility
            </a>
          </li>
          <li>
            <a href="/documentation" class="text-slate-600 hover:text-blue-600 transition-colors inline-flex items-center group">
              <span class="w-0 group-hover:w-2 h-0.5 bg-blue-600 transition-all mr-0 group-hover:mr-2"></span>
              Documentation
            </a>
          </li>
        </ul>
        
        <div class="mt-8">
          <a
            href="/rfq"
            class="inline-flex items-center px-6 py-3 text-sm font-semibold text-white bg-linear-to-r from-blue-600 to-blue-700 hover:from-blue-700 hover:to-blue-800 rounded-lg shadow-lg hover:shadow-xl transition-all"
          >
            Get Started
          </a>
        </div>
      </div>
    </div>

    <!-- Bottom Bar -->
    <div class="mt-16 pt-8 border-t border-slate-200">
      <div class="flex flex-col md:flex-row items-center justify-between space-y-4 md:space-y-0">
        <p class="text-sm text-slate-600">
           {t['footer.copyright'].replace('{year}', new Date().getFullYear().toString())}
        </p>
        <div class="flex items-center space-x-6 text-sm">
          <a href="/" class="text-slate-600 hover:text-blue-600 transition-colors">
            Privacy Policy
          </a>
          <span class="text-slate-300">•</span>
          <a href="/" class="text-slate-600 hover:text-blue-600 transition-colors">
            Terms of Service
          </a>
          <span class="text-slate-300">•</span>
          <a href="/" class="text-slate-600 hover:text-blue-600 transition-colors">
            Cookie Policy
          </a>
        </div>
      </div>
    </div>
  </div>
</footer>
